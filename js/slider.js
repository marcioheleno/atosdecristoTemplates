/** * Created by marciohelenomaiapessoa on 11/09/15. */sliderInt = 1;sliderNext = 2;$(document).ready(function(){    $(".slides ol > li#1").fadeIn(500);    starSlider();});function starSlider(){    count = $(".slides ol > li").size();    loop = setInterval(function(){        // faz a verificação tornando o loop        // o slider volta para a primeira possição        if(sliderNext > count){            sliderNext = 1;            sliderInt = 1;        }        $(".slides ol > li").fadeOut(500);        $(".slides ol > li#" + sliderNext).fadeIn(500);        sliderInt = sliderNext;        sliderNext = sliderNext + 1;    },3000)}function prev(){    newSlide = sliderInt - 1;    showSlide(newSlide);}function next(){    newSlide = sliderInt + 1;    showSlide(newSlide);}function stopLoop(){    // para o evento    window.clearInterval(loop);}function showSlide(id) {    // faz a verificação tornando o loop    // o slider volta para a primeira possição    stopLoop();    if(id > count){        id = 1;    } else if(id < 1){        id = count;    }    $(".slides ol > li").fadeOut(500);    $(".slides ol > li#" + id).fadeIn(500);    sliderInt = id;    sliderNext = id + 1;    starSlider();}$(".slides ol > li").mouseover(function(){    stopLoop();},function(){    starSlider();});